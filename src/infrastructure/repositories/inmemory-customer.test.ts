import {  InMemoryCustommerRepository } from "./inmemory-customer";


describe('In-memory customer repository', () => {

    const rep: InMemoryCustommerRepository = new InMemoryCustommerRepository()

    test('Adding item', () => {
        //expect(Customer.isvalidName('Daniel')).toBe(true)
        
    });

    test('Update item', () => {
        //expect(Customer.isvalidName('Daniel')).toBe(true)
        
    });

    test('Delete item', () => {
        //expect(Customer.isvalidName('Daniel')).toBe(true)
        
    });
    
})